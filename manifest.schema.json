{
  "type": "object",
  "properties": {
    "name": { "type": "string", "description": "The display name/title of this package." },
    "description": { "type": "string", "description": "Short summary of what the package does." },
    "author": { "type": "string" },
    "id": {"type": "string"},
    "install": { "type": "array", "minItems": 1, "items": {
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "enum": ["copy", "api", "link", "text-expander.espanso"],
        }
      },
      "required": ["method"]
    } },
    "remnoteVersion": { "type": "string" },
    "dependencies": { "type": "array" },
    "conflicts": { "type": "array" }
  },
  "required": ["name", "id", "install"] 
}
